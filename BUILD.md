# 🔨 LockBox - Инструкция по сборке и запуску

## 📋 Требования

### Системные требования:
- **OS:** macOS, Linux, Windows (WSL2)
- **RAM:** Минимум 4GB, рекомендуется 8GB
- **Disk:** Минимум 10GB свободного места

### Программное обеспечение:
- **Go:** версия 1.22 или новее
- **Git:** для клонирования репозитория
- **curl/jq:** (опционально) для тестирования API

---

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/dueldanov/lockbox.git
cd lockbox
```

### 2. Сборка

```bash
# Простая сборка
go build -o lockbox-node

# Проверка
ls -lh lockbox-node
# Должен показать: -rwxr-xr-x  44M  lockbox-node
```

**Время сборки:** 30-60 секунд на современном железе

### 3. Запуск

```bash
# Автоматический запуск (рекомендуется)
./start.sh

# Нода запустится и покажет:
# 🔒 Starting LockBox Node...
# 🚀 Launching LockBox Node...
# >>>>> Starting LockBox v2.0.2 <<<<<
# ...
# INFO RestAPI You can now access the API using: http://127.0.0.1:14265
```

### 4. Проверка

```bash
# Проверить статус
./status.sh

# Или вручную
curl http://127.0.0.1:14265/api/core/v2/info | jq .
```

**Ожидаемый ответ:**
```json
{
  "name": "LockBox",
  "version": "2.0.2",
  "protocol": {
    "networkName": "lockbox-devnet",
    "bech32Hrp": "lck"
  },
  "baseToken": {
    "name": "LockCoin",
    "tickerSymbol": "LOCK"
  }
}
```

---

## 📖 Подробная инструкция

### Шаг 1: Установка Go

#### macOS (Homebrew):
```bash
brew install go
go version  # Должно показать go1.22 или новее
```

#### Linux (Ubuntu/Debian):
```bash
wget https://go.dev/dl/go1.22.0.linux-amd64.tar.gz
sudo tar -C /usr/local -xzf go1.22.0.linux-amd64.tar.gz
export PATH=$PATH:/usr/local/go/bin
go version
```

#### Windows (WSL2):
Следуйте инструкциям для Linux выше.

### Шаг 2: Клонирование и подготовка

```bash
# Клонировать репозиторий
git clone https://github.com/dueldanov/lockbox.git
cd lockbox

# Проверить ветку
git branch
# Должно показать: * develop

# (Опционально) Обновить зависимости
go mod tidy
```

### Шаг 3: Сборка бинарника

```bash
# Сборка с оптимизацией
go build -o lockbox-node

# Проверка бинарника
file lockbox-node
# macOS: Mach-O 64-bit executable arm64
# Linux: ELF 64-bit LSB executable, x86-64
```

**Размер бинарника:** ~44MB

### Шаг 4: Первый запуск

#### Вариант A: Автоматический (рекомендуется)

```bash
# Запуск
./start.sh

# Скрипт автоматически:
# 1. Проверит наличие snapshot (создаст если нужно)
# 2. Проверит, не запущена ли нода
# 3. Удалит stale lock-файлы если нужно
# 4. Запустит ноду
```

#### Вариант B: Ручной

```bash
# 1. Создать genesis snapshot (только первый раз)
./lockbox-node tool snap-gen \
  --protocolParametersPath=protocol_parameters_devnet.json \
  --mintAddress=tst1qpszqzadsym6wpppd6z037dvlejmjuke7s24hm95s9fg9vpua7vlupxvxq2 \
  --treasuryAllocation=0 \
  --outputPath=lockbox_devnet_snapshots/full_snapshot.bin

# 2. Запустить ноду
./lockbox-node --config config_lockbox_devnet.json
```

### Шаг 5: Проверка работы

```bash
# Проверить процесс
ps aux | grep lockbox-node

# Проверить API
curl http://127.0.0.1:14265/health
# Ответ: (пустой = OK)

curl http://127.0.0.1:14265/api/core/v2/info | jq .
# Ответ: JSON с информацией о ноде
```

---

## 🛠️ Управление нодой

### Запуск
```bash
./start.sh
```

### Остановка
```bash
./stop.sh
```

### Проверка статуса
```bash
./status.sh
```

### Полная очистка (начать с нуля)
```bash
./clean.sh
# ⚠️ ВНИМАНИЕ: Удалит ВСЕ данные!
```

---

## ❓ Troubleshooting

### Проблема: "go: command not found"
**Решение:** Установите Go (см. Шаг 1)

### Проблема: "resource temporarily unavailable"
**Причина:** База данных заблокирована предыдущим процессом

**Решение:**
```bash
./stop.sh   # Остановит и очистит lock-файлы
./start.sh  # Запустит заново
```

### Проблема: "HORNET was not shut down properly"
**Причина:** База данных повреждена

**Решение:**
```bash
./start.sh  # Скрипт автоматически удалит повреждённую базу
```

### Проблема: Нода не отвечает на API
**Решение:**
```bash
# Подождите 5-10 секунд после старта
sleep 10
curl http://127.0.0.1:14265/health
```

---

## 📚 Дополнительная информация

- **[QUICKSTART.md](QUICKSTART.md)** - Быстрый старт
- **[README.md](README.md)** - Основная документация
- **[RESULT_RU.md](RESULT_RU.md)** - Итоговый отчёт

---

## ✅ Готово!

После успешного запуска вы можете:
- Использовать REST API на `http://127.0.0.1:14265`
- Интегрировать с вашим приложением
- Развернуть multi-node private tangle (см. `private_tangle/`)

