# 🔒 LockBox Node - Quick Start Guide

## 🚀 Быстрый старт

### 1. Запуск ноды

```bash
./start.sh
```

Скрипт автоматически:
- Проверит наличие snapshot (создаст если нужно)
- Проверит, не запущена ли нода уже
- Удалит stale lock-файлы если нужно
- Запустит LockBox Node

### 2. Проверка статуса

```bash
./status.sh
```

Покажет:
- Статус процесса (запущен/остановлен)
- Информацию о ноде (имя, версия, сеть, токен)
- Состояние базы данных
- Доступность REST API

### 3. Остановка ноды

```bash
./stop.sh
```

Корректно остановит ноду и очистит lock-файлы.

### 4. Полная очистка (начать с нуля)

```bash
./clean.sh
```

⚠️ **ВНИМАНИЕ:** Удалит ВСЕ данные (база, snapshots, p2p store)!

---

## 📡 API Endpoints

После запуска нода доступна на `http://127.0.0.1:14265`

### Проверка здоровья
```bash
curl http://127.0.0.1:14265/health
```

### Информация о ноде
```bash
curl http://127.0.0.1:14265/api/core/v2/info | jq .
```

Пример ответа:
```json
{
  "name": "LockBox",
  "version": "2.0.2",
  "protocol": {
    "networkName": "lockbox-devnet",
    "bech32Hrp": "lck"
  },
  "baseToken": {
    "name": "LockCoin",
    "tickerSymbol": "LOCK"
  }
}
```

---

## 🔧 Ручной запуск

Если хотите запустить вручную:

```bash
# 1. Создать snapshot (только первый раз)
./lockbox-node tool snap-gen \
  --protocolParametersPath=protocol_parameters_devnet.json \
  --mintAddress=tst1qpszqzadsym6wpppd6z037dvlejmjuke7s24hm95s9fg9vpua7vlupxvxq2 \
  --treasuryAllocation=0 \
  --outputPath=lockbox_devnet_snapshots/full_snapshot.bin

# 2. Запустить ноду
./lockbox-node --config config_lockbox_devnet.json
```

---

## 🛠️ Сборка

Пересобрать бинарник:

```bash
go build -mod=mod -o lockbox-node ./main.go
```

---

## 📁 Структура данных

```
lockbox/
├── lockbox-node                    # Бинарник
├── config_lockbox_devnet.json      # Конфигурация devnet
├── protocol_parameters_devnet.json # Параметры протокола
├── lockbox_devnet_db/              # База данных (создаётся автоматически)
├── lockbox_devnet_p2pstore/        # P2P хранилище (создаётся автоматически)
└── lockbox_devnet_snapshots/       # Snapshots (создаётся автоматически)
    └── full_snapshot.bin           # Genesis snapshot
```

---

## ❓ Troubleshooting

### "resource temporarily unavailable"

**Проблема:** База данных заблокирована другим процессом.

**Решение:**
```bash
./stop.sh  # Остановит ноду и очистит lock-файлы
./start.sh # Запустит заново
```

### "no download URL specified for snapshot files"

**Проблема:** Snapshot не создан.

**Решение:** Используйте `./start.sh` - он создаст snapshot автоматически.

### Нода не отвечает на API запросы

**Проверка:**
```bash
./status.sh  # Покажет статус API
```

Если нода запущена, но API не отвечает - подождите 5-10 секунд после старта.

---

## 🎯 Что дальше?

- Изучите [полную документацию](README.md)
- Настройте [private tangle](private_tangle/README.md) для multi-node setup
- Интегрируйте с вашим приложением через REST API

