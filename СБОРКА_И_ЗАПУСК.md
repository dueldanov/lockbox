# 🚀 LockBox - Сборка и запуск (Краткая инструкция)

## ⚡ Быстрый старт (3 команды)

```bash
# 1. Собрать
go build -o lockbox-node

# 2. Запустить
./start.sh

# 3. Проверить
curl http://127.0.0.1:14265/api/core/v2/info | jq .
```

**Готово!** Нода работает на `http://127.0.0.1:14265`

---

## 📋 Требования

- **Go 1.22+** (проверить: `go version`)
- **8GB RAM** (рекомендуется)
- **10GB disk** (свободного места)

---

## 🔨 Сборка

### Простая сборка:
```bash
go build -o lockbox-node
```

**Результат:**
```
-rwxr-xr-x  44M  lockbox-node
```

**Время:** 30-60 секунд

### Если ошибки:

#### "go: command not found"
```bash
# macOS
brew install go

# Linux
wget https://go.dev/dl/go1.22.0.linux-amd64.tar.gz
sudo tar -C /usr/local -xzf go1.22.0.linux-amd64.tar.gz
export PATH=$PATH:/usr/local/go/bin
```

#### "no required module provides package..."
```bash
go mod tidy
go build -o lockbox-node
```

---

## 🚀 Запуск

### Автоматический (рекомендуется):
```bash
./start.sh
```

**Что делает скрипт:**
1. Проверяет snapshot (создаёт если нужно)
2. Проверяет, не запущена ли нода
3. Удаляет stale lock-файлы
4. Запускает ноду

**Вывод:**
```
🔒 Starting LockBox Node...
🚀 Launching LockBox Node...
>>>>> Starting LockBox v2.0.2 <<<<<
...
INFO RestAPI You can now access the API using: http://127.0.0.1:14265
```

### Ручной запуск:
```bash
# Первый раз - создать snapshot
./lockbox-node tool snap-gen \
  --protocolParametersPath=protocol_parameters_devnet.json \
  --mintAddress=tst1qpszqzadsym6wpppd6z037dvlejmjuke7s24hm95s9fg9vpua7vlupxvxq2 \
  --treasuryAllocation=0 \
  --outputPath=lockbox_devnet_snapshots/full_snapshot.bin

# Запустить
./lockbox-node --config config_lockbox_devnet.json
```

---

## ✅ Проверка

### Проверить статус:
```bash
./status.sh
```

### Проверить API:
```bash
# Health check
curl http://127.0.0.1:14265/health

# Node info
curl http://127.0.0.1:14265/api/core/v2/info | jq .
```

**Ожидаемый ответ:**
```json
{
  "name": "LockBox",
  "version": "2.0.2",
  "protocol": {
    "networkName": "lockbox-devnet",
    "bech32Hrp": "lck"
  },
  "baseToken": {
    "name": "LockCoin",
    "tickerSymbol": "LOCK"
  }
}
```

---

## 🛑 Остановка

```bash
./stop.sh
```

---

## 🧹 Очистка (начать с нуля)

```bash
./clean.sh
# ⚠️ Удалит ВСЕ данные!
```

---

## ❓ Проблемы

### "resource temporarily unavailable"
**Решение:**
```bash
./stop.sh
./start.sh
```

### "HORNET was not shut down properly"
**Решение:**
```bash
./start.sh  # Автоматически удалит повреждённую базу
```

### Нода не отвечает
**Решение:**
```bash
# Подождать 5-10 секунд
sleep 10
curl http://127.0.0.1:14265/health
```

---

## 📚 Документация

- **[BUILD.md](BUILD.md)** - Подробная инструкция по сборке
- **[QUICKSTART.md](QUICKSTART.md)** - Быстрый старт с troubleshooting
- **[README.md](README.md)** - Основная документация

---

## ✅ Готово!

После успешного запуска:
- REST API: `http://127.0.0.1:14265`
- Network: `lockbox-devnet`
- Token: `LOCK`
- Bech32 HRP: `lck`

**Нода готова к использованию!** 🎉

